# React Native / Expo tooling can't resolve build-time deps (babel-preset-expo,
# @babel/runtime) from pnpm's isolated virtual store during release bundling on
# EAS. Hoisted layout is Expo's documented recommendation for pnpm projects.
node-linker=hoisted
