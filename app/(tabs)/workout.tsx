import { PlaceholderScreen } from '@/components/PlaceholderScreen';

export default function WorkoutScreen() {
  return <PlaceholderScreen title="Workout" note="Start and log workouts here — coming in Phase 2." />;
}
